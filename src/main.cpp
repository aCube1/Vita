#include "core/Engine.hpp"
#include "utils.hpp"

#include <cstdlib>

int main(int argc, char *argv[]) {
	VT_UNUSED(argc);
	VT_UNUSED(argv);

	auto engine = vt::core::Engine();
	// engine.run();

	return EXIT_SUCCESS;
}
